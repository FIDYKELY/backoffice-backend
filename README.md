# server-api
Server API for profily (nodejs, mongodb)

# installation
### Cloner le projet et installer les dependances
`git clone https://github.com/profilymg/server-api.git && cd server-api && npm i`

### Créer un fichier .env
server-api/.env

### Demarrer le serveur en local pour le dev
`npm start`
